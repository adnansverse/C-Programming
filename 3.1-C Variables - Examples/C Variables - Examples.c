//Calculate the Area of a Rectangle
#include <stdio.h>
int main() {
// Create variables
int length = 4;
int width = 6;
int area;

// Calculate the area
area = length * width;

// Print the result
printf("Length is: %d\n", length);
printf("Width is: %d\n", width);
printf("Area of the rectangle is: %d", area);
return 0;
}