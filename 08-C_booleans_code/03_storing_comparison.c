#include <stdio.h>
#include <stdbool.h>

int main() {
    int x = 10;
    int y = 9;

    // Store the result of a comparison in a boolean variable
    bool isGreater = x > y;

    printf("isGreater: %d\n", isGreater); // Prints 1 (true)

    return 0;
}
