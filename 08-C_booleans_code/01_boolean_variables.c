#include <stdio.h>
#include <stdbool.h>

int main() {
    // Create boolean variables
    bool isProgrammingFun = true;
    bool isFishTasty = false;

    // Print boolean values (1 for true, 0 for false)
    printf("Is Programming Fun? %d\n", isProgrammingFun);
    printf("Is Fish Tasty? %d\n", isFishTasty);

    return 0;
}
