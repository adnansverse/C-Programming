#include<stdio.h>
int main(){
int width,length,area;
printf("Enter the width of the garden :");
scanf("%d",&width);
printf("Enter the length of the garden :");
scanf("%d",&length);
area=width*length;
printf("The area of the garden :%d",area);

total_width=width+(2*width);
total_length=length+(2*length);
total_area=tatal_width*total_length;
printf("The total area covered by the garden and the surrounding road is :%d squre meters\n",total_area);










return 0;
}
